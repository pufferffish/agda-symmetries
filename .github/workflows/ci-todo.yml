name: "Run TODO to Issue"
on:
  push:
    branches:
      - master
      - main

jobs:
  build:
    runs-on: "ubuntu-latest"
    steps:
      - uses: "actions/checkout@v3"
      - name: "TODO to Issue"
        uses: "pufferffish/todo-to-issue-action@master"